namespace IMS.Api.Contracts.Locations
{
    // Response model for the CreateLocation endpoint, containing the ID of the newly created location.
    public sealed record CreateLocationResponse(int LocationId);
}
